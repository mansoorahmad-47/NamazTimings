<?xml version="1.0" encoding="utf-8"?>
<!-- The "already marked" state: outlined rather than filled, so the filled
     gold button always means "there is something to do". -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <corners android:radius="20dp" />
    <solid android:color="#224EC27F" />
    <stroke android:width="1dp" android:color="#554EC27F" />
</shape>
